<h1>Listing users</h1>

<table>
  <tr>
    <th>Email</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %> [<%= link_to 'show', user %>]</td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_registration_path %>
